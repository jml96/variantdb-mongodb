cd
cd Documents/Brompton/variantdb_mongodb/
conda update -n base -c conda-forge conda
conda env create -f environment.yml
conda activate variantdb_mongodb
# edit variants/data/sample.csv and variants/data/variants.csv to update sample/variant data
# or set DATA_SOURCE = "mongodb" in variantdb/settings.py to read from MongoDB instead (see README section 4)
python manage.py migrate
python manage.py runserver
# to refresh the site with new CSV data later, just edit the CSVs and rerun:
python manage.py migrate